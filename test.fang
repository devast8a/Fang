class Foo {
    val x: int

    fn test(mut self: Foo){
        writeLn("Value %d\n", self.x)
    }

    fn destructor(own self: Foo){
        writeLn("Destroy Foo %d\n", self.x)
    }
}

fn test(mut b: Foo, mut c: Foo){

}

fn main(){
    val a: Foo = Foo{}
    val b: Foo = Foo{}

    a.x = 10
    b.x = 20

    # test(a, a)
    # test(a, a)

    Foo.test(a)
    Foo.test(b)

    val x: int = 0
    val y: int = 20

    if(x){
        Foo.test(a)
        Foo.test(a)
    } else {
        Foo.test(b)
    }
}