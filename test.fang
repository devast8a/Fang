trait Printable {
    fn print(): none
}

class Foo impl Printable {
    fn print(): none {
        writeLn("I am Foo!\n", 0)
    }
}

class Bar impl Printable {
    fn print(): none {
        writeLn("I am Bar!\n", 0)
    }
}

fn dispatch(p: Printable): none {
    p.print()
}

fn main(): none {
    val foo: Foo
    val bar: Bar

    foo.print()
    bar.print()
    dispatch(foo)
    dispatch(bar)
}
