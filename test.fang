class Foo {
    val x: int

    fn test(mut self: Foo){
        writeLn("Value %d\n", self.x)
    }
}

fn main(){
    val a: Foo = Foo{}
    val b: Foo = Foo{}

    a.x = 10
    b.x = 20

    Foo.test(a)
    Foo.test(b)

    a.test()

    writeLn("Hello world\n", 0)
}