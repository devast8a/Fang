trait Animal {
    fn speak() -> str
}

struct Cat impl Animal {
    fn speak() => 'Meow!'
}

struct Dog impl Animal {
    fn speak() => 'Bark!'
}

fn speak(animal: Animal) => animal.speak()

return speak(Cat{}) == 'Meow!' and
    speak(Dog{}) == 'Bark!'