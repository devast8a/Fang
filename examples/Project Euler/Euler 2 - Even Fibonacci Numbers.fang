mut prev = 1
mut current = 1

mut sum = 0

while current < 4_000_000 {
    # Get the next fibonacci number
    val prev_ = prev
    prev = current
    current = prev_ + current

    if current%2 == 0 {
        sum = sum + current
    }
}

return sum == 4613732